import 'color_theme.dart';

class ColorThemes {
  ColorThemes._();

  // TODO: Pre-defined color themes, categorized into sub-classes.

  /// A map containing every theme included in the [ColorThemes] class,
  /// with their respective names as their keys.
  static const Map<String, ColorTheme> all = <String, ColorTheme>{
    // TODO:
  };
}
